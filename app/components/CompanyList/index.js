import React, { Component } from 'react';
import {
    View,
    Text,
    FlatList,
} from 'react-native';

export default class CompanyList extends Component {
    render() {
        return (
            <FlatList

            />
        );
    }
}