import TYPES from '../constants';

const initState = {
    isfetching: false,

}
function postData(state = initState, action) {
    switch(action.type) {
        case TYPES.REQUEST_START:
            return 
    }
}

function userInfo( state = {}, action) {
    switch(action.type) {
        case 
    }
}